<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.9.xsd">
    <changeSet id="create-tables" author="00019278">
        <sql>

            ALTER TABLE timetable.subject add column if not exists color TEXT;
            ALTER TABLE timetable.subject add column if not exists emoji TEXT;
            ALTER TABLE timetable.subject add column if not exists weight integer;
        </sql>
    </changeSet>
</databaseChangeLog>
